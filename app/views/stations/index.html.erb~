<b class="notice"><%= notice %></b>
<h1> Listing power stations </h1>
<div> Regional Directorate <%= select_tag "rd_code", options_for_select(regional_directorates.push('All'), "All") %> </div> 
<div> Accounting Office <%#= select_tag "acc_office", options_for_select(acc_offices(s.push('All'), "All") %> </div> 	
<div id="box">

	<ul>
	<% @stations.each do |station| %>
			<li>
				<%= link_to station.name, station_batteries_path(station) %> 
				<%= link_to 'Edit', edit_station_path(station) %> | <%= link_to 'Delete', station, method: :delete, data: { confirm: 'Are you sure?' } %>
			</li>
	<% end %>
	</ul>   
	
	

</div>

