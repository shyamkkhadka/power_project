<h1>Listing banks</h1>

<table class="stations">
  <tr>
    <th>Total voltage</th>
    <th>Visual inpsection</th>
    <th>Cells</th>
    <th>Battery</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @banks.each do |bank| %>
  <tr>
    <td><%= bank.total_voltage %></td>
    <td><%= bank.visual_inpsection %></td>
    <td><%= bank.cells %></td>
    <td><%= bank.battery_id %></td>
    <td><%= link_to 'Show', bank %></td>
    <td><%= link_to 'Edit', edit_bank_path(bank) %></td>
    <td><%= link_to 'Destroy', bank, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Bank', new_bank_path %>
